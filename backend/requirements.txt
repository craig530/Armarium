fastapi==0.104.1
uvicorn[standard]==0.24.0
sqlalchemy[asyncio]==2.0.23
alembic==1.18.4
aiosqlite==0.19.0
httpx==0.25.2
python-multipart==0.0.6
pillow==10.1.0
python-dotenv==1.0.0
pydantic==2.5.2
pydantic-settings==2.1.0
aiofiles==23.2.1

# Server-side barcode decoding for the camera scanner (the zxing-cpp port —
# far more robust at decoding real camera frames than the @zxing/library JS
# port used previously). Prebuilt wheels cover cp311 manylinux x86_64 and
# aarch64, matching this project's python:3.11-slim image on both
# architectures, so no compiler is needed in the Docker build.
zxing-cpp==3.0.0

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
# bcrypt>=4.1 raises ValueError on passlib's internal self-test (>72 byte
# input), which crashes auth entirely. Pin to the last compatible release.
bcrypt==4.0.1

# Testing
pytest==7.4.3
pytest-asyncio==0.23.2
httpx==0.25.2
anyio==3.7.1
